import { Router } from 'express';

const router = Router();

// Auth routes
// router.post('/auth/login', authController.login);
// router.post('/auth/refresh', authController.refresh);

// Projects routes
// router.get('/projects', projectController.list);
// router.post('/projects', projectController.create);
// router.get('/projects/:id', projectController.findById);

// Tasks routes
// router.get('/projects/:projectId/tasks', taskController.list);
// router.post('/projects/:projectId/tasks', taskController.create);
// router.patch('/projects/:projectId/tasks/:taskId', taskController.update);
// router.delete('/projects/:projectId/tasks/:taskId', taskController.delete);
// router.get('/projects/:projectId/tasks/:taskId/history', taskController.getHistory);

// Analytics routes
// router.get('/projects/:projectId/analytics/summary', analyticsController.getSummary);

// Users routes
// router.get('/users', userController.list);
// router.post('/users/:userId/roles', userController.assignRole);

export default router;
